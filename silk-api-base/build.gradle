version = project.silk_api_base_version + "+" + rootProject.version

repositories {
	mavenCentral()
}

dependencies {
	api project(path: ":silk-api-annotation-processor", configuration: "namedElements")
}